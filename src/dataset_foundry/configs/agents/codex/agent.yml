container:
  image: "codex-agent:latest"
  environment:
    OPENAI_API_KEY: "${OPENAI_API_KEY}"
  command: ["printenv OPENAI_API_KEY | codex login --with-api-key && cat input/prompt.md | codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check"]
